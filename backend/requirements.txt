# ---- Web framework ----
flask==3.0.3
flask-cors==4.0.1
python-dotenv==1.0.1
werkzeug==3.0.3

# ---- Deep learning (CPU) ----
# NOTE: installs CPU-only torch by default. On some systems, use:
# pip install torch==2.3.1+cpu --index-url https://download.pytorch.org/whl/cpu
torch==2.3.1
torchvision==0.18.1
torchaudio==2.3.1

# ---- HuggingFace pretrained deepfake detectors ----
transformers==4.44.2
huggingface-hub==0.24.6
safetensors==0.4.4

# ---- Media preprocessing ----
opencv-python-headless==4.10.0.84
librosa==0.10.2.post1
soundfile==0.12.1
numpy==1.26.4
scipy==1.13.1
pillow==10.4.0

# ---- Face detection (light, CPU-friendly) ----
# mediapipe is optional but recommended for face detection; falls back to OpenCV Haar if missing.
mediapipe==0.10.14 ; platform_system != "Linux" or python_version < "3.12"

# ---- Utilities ----
tqdm==4.66.5
scikit-learn==1.5.1
pydub==0.25.1
